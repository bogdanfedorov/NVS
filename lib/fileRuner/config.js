export var fsReadFileConfig = {
  encoding: "utf8",
};

export var vmContextOptions = {
  codeGeneration: {
    strings: false,
    wasm: false,
  },
};

export var vmContext = {
  result: null,
  setTimeout,
};
